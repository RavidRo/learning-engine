interface:
  display_name: "Spec Ticket"
  short_description: "Move a Linear ticket to In Progress and create an OpenSpec plan"
  default_prompt: "Use $spec-ticket to move a Linear ticket to In Progress, grill the requirements, and propose an OpenSpec change."
