## 1. Updates Page UI

- [x] 1.1 Add a small source metadata image renderer that uses `source_interest.source_image_url` and hides missing or failed images.
- [x] 1.2 Render the small source image beside the source label/type metadata without changing the existing update thumbnail behavior.
- [x] 1.3 Update responsive styles so source metadata images, labels, timestamps, thumbnails, and save controls remain readable without overlap.

## 2. Verification

- [x] 2.1 Run the narrowest useful webapp checks for the changed frontend files.
- [x] 2.2 Run `task check` if feasible before finishing.
