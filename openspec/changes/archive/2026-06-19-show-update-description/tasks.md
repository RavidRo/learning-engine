## 1. Update Card Rendering

- [x] 1.1 Add a small rendering helper or equivalent logic so `UpdateItem` treats missing, null, and whitespace-only summaries as no description.
- [x] 1.2 Render a non-blank update summary directly below the update title and above the existing metadata.

## 2. Styling

- [x] 2.1 Add update-card description styling that is subordinate to the title, readable, and constrained for long summaries.
- [x] 2.2 Verify the existing title, metadata, timestamp, thumbnail, and collection action layout still fits on narrow viewports.

## 3. Verification

- [x] 3.1 Run the narrowest useful webapp verification for the presentation change.
- [x] 3.2 Run `task check` if feasible before finishing.
