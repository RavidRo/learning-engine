## 1. Backend update image support

- [x] 1.1 Add optional update-specific image URL support to collected update models.
- [x] 1.2 Extract feed item images from common feed metadata while preserving existing filtering behavior.
- [x] 1.3 Add backend tests for feed item image extraction and update payload propagation.

## 2. Webapp thumbnail priority

- [x] 2.1 Accept optional update image URLs in the Updates API schema.
- [x] 2.2 Render update thumbnails with priority: update image, source image, fallback initial.

## 3. OpenSpec completion

- [x] 3.1 Sync the delta spec into the canonical source image resolution spec.
- [x] 3.2 Archive the completed change.
